xlanguage: python

python:
  - "3.7"

install:
  - pip install -r requirements.txt

script:
  - python script_do_integrante.py
  - python outro_script_do_integrante.py
  # Adicione uma linha para cada script dos integrantes

